CREATE OR REPLACE FUNCTION {{routineName}}({{pkName}} {{pkType}}) RETURNS INT LANGUAGE plpgsql AS $$
DECLARE
  affected INT;
BEGIN
  DELETE FROM {{tableTok}} t WHERE t.{{pkName}} = {{routineName}}.{{pkName}};
  GET DIAGNOSTICS affected = ROW_COUNT;
  RETURN affected;
END;
$$;
